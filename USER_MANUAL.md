# 組織図作成アプリケーション ユーザーマニュアル

## 目次

1. [はじめに](#はじめに)
2. [アプリケーションの起動](#アプリケーションの起動)
3. [画面構成](#画面構成)
4. [基本操作](#基本操作)
5. [社員管理](#社員管理)
6. [組織管理](#組織管理)
7. [組織図の表示と操作](#組織図の表示と操作)
8. [データの保存と読み込み](#データの保存と読み込み)
9. [HTML出力](#html出力)
10. [よくある質問](#よくある質問)
11. [トラブルシューティング](#トラブルシューティング)

---

## はじめに

組織図作成アプリケーションは、企業や組織の階層構造を視覚的に作成・管理できるデスクトップアプリケーションです。

### 主な機能

- **社員管理**: 社員の基本情報（氏名、役職、部署など）を登録・編集・削除
- **組織管理**: 組織（部署）の情報を階層構造で管理
- **組織図表示**: 社員と組織の関係を視覚的に表示
- **HTML出力**: 組織図をスタンドアロンHTMLファイルとして出力
- **データ保存**: JSON形式でデータを保存・読み込み

---

## アプリケーションの起動

### 初回起動

1. コマンドプロンプトまたはターミナルを開く
2. プロジェクトディレクトリに移動
3. 以下のコマンドを実行:

```bash
npm start
```

4. アプリケーションウィンドウが表示されます

### サンプルデータについて

初回起動時には、8名の社員と4つの組織のサンプルデータが読み込まれています。これを使ってアプリケーションの機能を試すことができます。

---

## 画面構成

アプリケーションは以下の要素で構成されています：

### 1. トップバー（上部）
- **アプリケーション名**: 「組織図作成アプリ」と表示
- **ファイル操作ボタン**:
  - 新規: 新しいデータを作成
  - 開く: 保存したファイルを読み込み
  - 保存: 現在のデータを保存

### 2. サイドバー（左側）
4つのメニュー項目があります：
- ダッシュボード: 統計情報とクイックアクション
- 社員管理: 社員データの管理
- 組織管理: 組織データの管理
- 組織図表示: 組織図の表示とHTML出力

### 3. メインコンテンツエリア（中央）
選択したメニューに応じた画面が表示されます。

---

## 基本操作

### メニューの切り替え

左サイドバーのメニュー項目をクリックすると、対応する画面に切り替わります。

### モーダルダイアログの操作

社員や組織を追加・編集する際は、モーダルダイアログ（ポップアップウィンドウ）が表示されます。

- **保存**: 入力内容を保存してモーダルを閉じる
- **キャンセル**: 変更を破棄してモーダルを閉じる
- **×ボタン**: モーダルを閉じる（キャンセルと同じ）

### 通知メッセージ

操作が成功または失敗すると、画面右下に通知メッセージ（トースト）が表示されます。

- 緑色: 成功
- 赤色: エラー

---

## 社員管理

### 社員を追加する

1. サイドバーから「社員管理」をクリック
2. 「社員を追加」ボタンをクリック
3. モーダルダイアログに以下の情報を入力:

#### 必須項目（* 印）
- **社員ID**: 一意の識別子（例: EMP009）
- **氏名**: 社員の氏名
- **役職**: 役職名（例: 課長、主任、担当）
- **部署**: 所属部署名

#### 任意項目
- **上司ID**: 直属の上司の社員ID（空欄の場合は最上位として扱われます）
- **メールアドレス**: メールアドレス
- **電話番号**: 電話番号
- **備考**: その他の情報

4. 「保存」ボタンをクリック

### 社員を編集する

1. 社員管理画面で、編集したい社員の行にある「編集」ボタンをクリック
2. モーダルダイアログで情報を修正
3. 「保存」ボタンをクリック

**注意**: 社員IDは編集できません

### 社員を削除する

1. 社員管理画面で、削除したい社員の行にある「削除」ボタンをクリック
2. 確認ダイアログで「OK」をクリック

### 社員を検索する

社員管理画面上部の検索ボックスに、以下の情報を入力すると絞り込み検索ができます：
- 社員ID
- 氏名
- 役職
- 部署

---

## 組織管理

### 組織を追加する

1. サイドバーから「組織管理」をクリック
2. 「組織を追加」ボタンをクリック
3. モーダルダイアログに以下の情報を入力:

#### 必須項目（* 印）
- **組織ID**: 一意の識別子（例: ORG005）
- **組織名**: 組織（部署）の名称

#### 任意項目
- **親組織ID**: 上位組織のID（空欄の場合はトップレベル組織として扱われます）
- **説明**: 組織の説明
- **色**: 組織図での識別用カラー（カラーピッカーで選択）

4. 「保存」ボタンをクリック

### 組織を編集する

1. 組織管理画面で、編集したい組織の「編集」ボタンをクリック
2. モーダルダイアログで情報を修正
3. 「保存」ボタンをクリック

**注意**: 組織IDは編集できません

### 組織を削除する

1. 組織管理画面で、削除したい組織の「削除」ボタンをクリック
2. 確認ダイアログで「OK」をクリック

**注意**: 子組織が存在する組織は削除できません。先に子組織を削除または移動させてください。

### 組織の階層構造

組織管理画面では、組織が階層構造（ツリー形式）で表示されます。親組織の下に子組織がインデントされて表示されます。

---

## 組織図の表示と操作

### 組織図を表示する

1. サイドバーから「組織図表示」をクリック
2. 社員データに基づいて組織図が自動的に生成されます

### 組織図の構造

- **階層**: 上司-部下の関係に基づいて自動的に階層が構成されます
- **トップレベル**: 上司が設定されていない社員、または上司が存在しない社員が最上位に表示されます
- **各レベル**: 同じ階層の社員は横並びで表示されます

### ズーム操作

組織図が大きい場合や小さい場合は、ズーム機能を使用できます：

- **拡大**: 「拡大」ボタンをクリック（最大3倍まで）
- **縮小**: 「縮小」ボタンをクリック（最小0.5倍まで）
- **リセット**: 「リセット」ボタンで元の大きさに戻る

### ノードの表示内容

各社員ノードには以下の情報が表示されます：
- 氏名（太字）
- 役職
- 部署

ノードの枠線の色は、所属組織の設定色に基づいて変わります。

---

## データの保存と読み込み

### 新規ファイルを作成する

1. トップバーの「新規」ボタンをクリック
2. 確認ダイアログで「OK」をクリック
3. 全てのデータがクリアされます

**注意**: 保存していないデータは失われます

### データを保存する

#### 初回保存時
1. トップバーの「保存」ボタンをクリック
2. ファイル保存ダイアログが表示される
3. 保存場所とファイル名を指定（拡張子は .json）
4. 「保存」をクリック

#### 2回目以降
1. トップバーの「保存」ボタンをクリック
2. 前回保存したファイルに上書き保存されます

### データを読み込む

1. トップバーの「開く」ボタンをクリック
2. ファイル選択ダイアログが表示される
3. 読み込みたいJSONファイルを選択
4. 「開く」をクリック
5. データが読み込まれます

**注意**: 現在編集中のデータは失われます（保存していない場合）

### データ形式

データはJSON形式で保存されます。以下の構造を持ちます：

```json
{
  "employees": [ /* 社員データの配列 */ ],
  "organizations": [ /* 組織データの配列 */ ],
  "savedAt": "2025-01-01T12:00:00.000Z"
}
```

---

## HTML出力

### 組織図をHTMLファイルとして出力する

1. 組織図表示画面で「HTML出力」ボタンをクリック
2. ファイル保存ダイアログが表示される
3. 保存場所とファイル名を指定（拡張子は .html）
4. 「保存」をクリック
5. HTMLファイルが生成されます

### 出力されるHTMLファイルの特徴

- **スタンドアロン**: 単一のHTMLファイルで完結（外部ファイル不要）
- **スタイル埋め込み**: CSSが埋め込まれているため、どこでも表示可能
- **静的**: JavaScriptを使用していないため、セキュリティリスクが低い
- **印刷対応**: ブラウザの印刷機能で印刷可能
- **レスポンシブ**: 画面サイズに応じて自動調整

### HTMLファイルの使い方

1. 生成されたHTMLファイルをダブルクリック
2. Webブラウザで開かれ、組織図が表示されます
3. そのまま印刷したり、メールで共有したりできます

---

## よくある質問

### Q1: サンプルデータを削除できますか？

**A**: はい、トップバーの「新規」ボタンをクリックすると全データがクリアされます。または、社員管理・組織管理画面から個別に削除できます。

### Q2: 社員IDや組織IDのルールはありますか？

**A**: 特にルールはありませんが、一意である必要があります。推奨形式は「EMP001」「ORG001」のような英数字の組み合わせです。

### Q3: 上司が存在しない社員IDを上司IDに設定するとどうなりますか？

**A**: その社員はトップレベルとして扱われます。エラーにはなりません。

### Q4: 組織図の見た目をカスタマイズできますか？

**A**: 現在のバージョンでは、組織ごとの色設定のみ可能です。詳細なカスタマイズには、出力されたHTMLファイルを直接編集する必要があります。

### Q5: 大規模な組織（100人以上）でも使えますか？

**A**: はい、1000人規模まで対応していますが、組織図の表示が複雑になる可能性があります。ズーム機能を活用してください。

### Q6: データのバックアップは取られますか？

**A**: 自動バックアップ機能はありません。定期的に「保存」ボタンでデータを保存してください。

### Q7: 複数の組織図を管理できますか？

**A**: はい、異なるファイル名でJSONファイルを複数保存することで、複数の組織図を管理できます。

### Q8: ExcelやCSVからデータをインポートできますか？

**A**: 現在のバージョンでは非対応です。JSONファイルの直接編集により可能ですが、手動での作業となります。

---

## トラブルシューティング

### 問題: アプリケーションが起動しない

**解決策**:
1. Node.jsがインストールされているか確認: `node --version`
2. 依存パッケージがインストールされているか確認: `npm install`
3. エラーメッセージを確認し、不足しているパッケージをインストール

### 問題: データを保存しても反映されない

**解決策**:
1. 保存成功の通知メッセージが表示されたか確認
2. ファイルの保存場所を確認
3. ファイルのアクセス権限を確認

### 問題: 組織図が表示されない

**解決策**:
1. 社員データが登録されているか確認（ダッシュボードで確認）
2. ブラウザをリフレッシュ（アプリを再起動）
3. 循環参照がないか確認（AさんがBさんの上司で、BさんがAさんの上司、など）

### 問題: 社員IDが重複していると言われる

**解決策**:
1. 社員管理画面で既存の社員IDを確認
2. 重複しない新しいIDを使用

### 問題: 組織が削除できない

**解決策**:
1. 子組織が存在しないか確認
2. 存在する場合は、先に子組織を削除または別の親組織に移動

### 問題: HTML出力がうまくいかない

**解決策**:
1. 保存先のディレクトリに書き込み権限があるか確認
2. ファイル名に使用できない文字（/、\、:など）が含まれていないか確認
3. ディスクの空き容量を確認

### 問題: 日本語が文字化けする

**解決策**:
1. 保存したJSONファイルがUTF-8エンコーディングか確認
2. HTMLファイルをブラウザで開いた際、ブラウザのエンコーディング設定を確認

---

## サポート

問題が解決しない場合は、以下の情報を含めて開発チームにお問い合わせください：

- 発生した問題の詳細
- エラーメッセージ（あれば）
- 操作手順
- 使用しているOS
- Node.jsのバージョン

---

**最終更新**: 2025年10月27日
**バージョン**: 1.0.0
